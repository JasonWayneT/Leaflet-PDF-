import React from 'react'

// Implements ARCH-001: root React component — InputScreen replaces this in Story 3.5
function App(): React.ReactElement {
  return <h1>Bookit v2</h1>
}

export default App
